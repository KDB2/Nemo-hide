First install the dependencies.
In Debian/Ubuntu, the (known) dependencies are:
- cmake
- python-nemo

Then open a terminal in the project directory and run:
$ mkdir build
$ cd build
$ cmake ..
$ make
$ sudo make install

Finally, restart Nemo if it's running:
$ nemo -q
Then start it again.
